{
  "name": "hk-heights",
  "type": "module",
  "private": true,
  "engines": {
    "node": "18.x || 20.x"
  },
  "scripts": {
    "build": "prisma generate && nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare && prisma generate",
    "verify-production": "node verify-production.js",
    "deploy": "npm run build && netlify deploy --prod --dir=.output/public"
  },
  "dependencies": {
  "@prisma/client": "^6.14.0",
  "bcryptjs": "^3.0.2",
  "formidable": "^3.5.4",
  "nuxt": "latest",
  "pg": "^8.16.3",
  "prisma": "^6.14.0",
  "vite-tsconfig-paths": "^5.1.4",
  "vue": "^3.5.18",
  "vue-router": "^4.5.1"
  }
}
